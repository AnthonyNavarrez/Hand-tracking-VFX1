// Intentionally blank stub — Tool 2's real implementation is out of scope
// for this plan. Needs its own opaque layer so the shared camera video
// (a fixed, full-viewport layer owned by CameraProvider) doesn't show
// through a route with no content of its own.
function ToolTwoStub() {
  return <div style={{ position: 'relative', width: '100vw', height: '100vh', background: '#000' }} />;
}

export default ToolTwoStub;
